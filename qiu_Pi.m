% 投石法（蒙托卡诺法，即借助随机数的方法）求Π
% 正方形内切圆，扔N颗石子，落在圆内M颗，S圆/S方=M/N=Π/4
% 建立坐标系，x,y符合-1到1的均匀分布，找x^2+y^2≤1的数目M
clear;
clc;
% M=0;
% N=1e7;
% x=2*rand(1,N)-1;
% y=2*rand(1,N)-1;
% s=x.^2+y.^2;
% %也可以用极坐标，即s=x+y*1i，M=sum(abs(s)<=1);即计算模
% M=sum(s<=1);   %注意这里不能直接写if s<1，因为这要求s数组中所有项都小于1
% pai=(4*M)/N %注意这里不能写成pi，可能会和matlab自带变量pi冲突

%利用循环(想好循环体是谁，用谁来循环）
N=1e7;
x=2*rand(1,N)-1;
y=2*rand(1,N)-1;
M=0;
for k=1:N
    s=x(k)^2+y(k)^2;
    if s<=1
        M=M+1;
    end
end
pai=4*M/N